pages:
  image: node:22
  stage: deploy
  variables:
    VUE_APP_REPO_URL: "https://gitlab.example.com/"
  script:
    - npm install
    - npm ci
    - npm run build
    - mv public public-vue
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - main
